_schema-version: '3.1'
ID: btp-cost-allocation
version: 1.0.0
description: SAP BTP Cost Allocation & Showback Engine — advisory app

parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
        - echo "omit=dev" > gen/srv/.npmrc
        - npm ci --prefix app/cost-dashboard

modules:
  - name: cost-ui
    type: html5
    path: app/cost-dashboard
    build-parameters: { builder: custom, commands: [npm run build], supported-platforms: [] }

  - name: cost-ui-deployer
    type: com.sap.application.content
    path: .
    requires: [{ name: cost-html5-repo-host, parameters: { content-target: true } }]
    build-parameters:
      build-result: resources/
      requires: [{ name: cost-ui, artifacts: [cost-dashboard.zip], target-path: resources/ }]

  - name: cost-srv
    type: nodejs
    path: gen/srv
    parameters: { memory: 512M, disk-quota: 1024M, instances: 1 }
    requires:
      - name: cost-xsuaa
      - name: cost-cis
      - name: cost-uas
    provides:
      - name: cost-srv-api
        public: true
        properties: { srv-url: ${default-url} }

  - name: cost-destination-content
    type: com.sap.application.content
    requires:
      - { name: cost-destination-service, parameters: { content-target: true } }
      - { name: cost-xsuaa, parameters: { service-key: { name: cost-xsuaa-key } } }
      - { name: cost-html5-repo-host, parameters: { service-key: { name: cost-html5-repo-host-key } } }
    parameters:
      content:
        instance:
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: cost-srv-destination
              HTML5.ForwardAuthToken: true
              TokenServiceInstanceName: cost-xsuaa
              TokenServiceKeyName: cost-xsuaa-key
              URL: ~{cost-srv-api/srv-url}
              sap.cloud.service: com.btpconsulting.costallocation
          existing_destinations_policy: update
    build-parameters: { no-source: true }

resources:
  - name: cost-xsuaa
    type: com.sap.xs.uaa
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: btp-cost-allocation-${org}-${space}
        tenant-mode: dedicated

  - name: cost-cis
    type: org.cloudfoundry.managed-service
    parameters: { service: cis, service-plan: central }

  - name: cost-uas
    type: org.cloudfoundry.managed-service
    parameters: { service: uas, service-plan: reporting-ga-admin }

  - name: cost-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters: { service: html5-apps-repo, service-plan: app-host }

  - name: cost-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - { Name: ui5, Description: SAP UI5 CDN, URL: https://ui5.sap.com, Type: HTTP, ProxyType: Internet, Authentication: NoAuthentication }
