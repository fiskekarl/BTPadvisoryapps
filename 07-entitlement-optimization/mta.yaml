_schema-version: '3.1'
ID: btp-entitlement-optimization
version: 1.0.0
description: SAP BTP Entitlement & License Optimization Advisor — advisory app

parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
        - echo "omit=dev" > gen/srv/.npmrc
        - npm ci --prefix app/entitle-dashboard

modules:
  - name: entitle-ui
    type: html5
    path: app/entitle-dashboard
    build-parameters: { builder: custom, commands: [npm run build], supported-platforms: [] }

  - name: entitle-ui-deployer
    type: com.sap.application.content
    path: .
    requires: [{ name: entitle-html5-repo-host, parameters: { content-target: true } }]
    build-parameters:
      build-result: resources/
      requires: [{ name: entitle-ui, artifacts: [entitle-dashboard.zip], target-path: resources/ }]

  - name: entitle-srv
    type: nodejs
    path: gen/srv
    parameters: { memory: 512M, disk-quota: 1024M, instances: 1 }
    requires:
      - name: entitle-xsuaa
      - name: entitle-cis
      - name: entitle-uas
    provides:
      - name: entitle-srv-api
        public: true
        properties: { srv-url: ${default-url} }

  - name: entitle-destination-content
    type: com.sap.application.content
    requires:
      - { name: entitle-destination-service, parameters: { content-target: true } }
      - { name: entitle-xsuaa, parameters: { service-key: { name: entitle-xsuaa-key } } }
      - { name: entitle-html5-repo-host, parameters: { service-key: { name: entitle-html5-repo-host-key } } }
    parameters:
      content:
        instance:
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: entitle-srv-destination
              HTML5.ForwardAuthToken: true
              TokenServiceInstanceName: entitle-xsuaa
              TokenServiceKeyName: entitle-xsuaa-key
              URL: ~{entitle-srv-api/srv-url}
              sap.cloud.service: com.btpconsulting.entitlement
          existing_destinations_policy: update
    build-parameters: { no-source: true }

resources:
  - name: entitle-xsuaa
    type: com.sap.xs.uaa
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: btp-entitlement-optimization-${org}-${space}
        tenant-mode: dedicated

  - name: entitle-cis
    type: org.cloudfoundry.managed-service
    parameters: { service: cis, service-plan: central }

  # Same UAS plan as BTPbilling — needed for monthlyUsage history.
  - name: entitle-uas
    type: org.cloudfoundry.managed-service
    parameters: { service: uas, service-plan: reporting-ga-admin }

  - name: entitle-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters: { service: html5-apps-repo, service-plan: app-host }

  - name: entitle-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - { Name: ui5, Description: SAP UI5 CDN, URL: https://ui5.sap.com, Type: HTTP, ProxyType: Internet, Authentication: NoAuthentication }
